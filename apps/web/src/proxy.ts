/**
 * DEPRECATED: this file was previously named `proxy.ts` in the mistaken
 * belief that Next.js 16 renamed `middleware.ts` to `proxy.ts`. Next.js
 * (and Vercel) still require the middleware file to be named
 * `middleware.ts` (or `src/middleware.ts`).
 *
 * The real middleware is now `apps/web/src/middleware.ts`. This file is
 * kept only to avoid breaking imports during the transition and should be
 * deleted in a follow-up commit.
 */
